apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.appName }}
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}